[package]
name = "template"
version = "0.0.1"
entrypoint = "src/lib.typ"
authors = [
  "Clemens Koza <https://github.com/SillyFreak/>"
]
license = "MIT"
description = "template package"
repository = "https://github.com/SillyFreak/typst-packages/template"
keywords = []
# compiler = ""
exclude = [
  # manual and gallery are not needed in the final package
  "manual.typ",
  "manual.pdf",
  "gallery/*",
]
